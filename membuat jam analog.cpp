#include<cmath>
#include<cstdlib>
#include<cstring>
#include<Transform.h>
TPoint newPoint(int x, int y)
{
	TPoint np={x,y};
	return np;
}
struct TMatrix loadldentity(int x, in dimensi)
{
	TMatrix m;
	for(int i=0;i<=dimensi;i++)
}
for